# Core API
fastapi
uvicorn[standard]
python-multipart
beautifulsoup4

# Screenshot & CV analysis (Module 3)
Pillow
opencv-python-headless
numpy

# Database
aiosqlite

# Runtime utilities
typing_extensions

# Embedding similarity scoring (Module 3b — optional, enables /benchmark on screenshots)
# Uncomment to enable CLIP similarity scoring; kept out of Docker build
# because PyTorch adds ~2GB. Run: pip install sentence-transformers
# sentence-transformers
