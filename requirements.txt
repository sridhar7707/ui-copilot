# Core API
fastapi
uvicorn[standard]
python-multipart
beautifulsoup4

# Screenshot & CV analysis (Module 3)
Pillow
opencv-python-headless
numpy

# Embedding similarity scoring (Module 3b — CLIP via sentence-transformers)
sentence-transformers

# --- Database (add when implementing repositories) ---
aiosqlite

# --- Runtime utilities ---
typing_extensions

# --- Dev & quality tooling ---
pytest
ruff
pytest-asyncio
httpx
